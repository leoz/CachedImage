name: "CodeQL config for CachedImage"

packs:
  - codeql/swift-queries

build:
  command: |
    xcodebuild build \
      -project CachedImage/Demo/CachedImageDemo.xcodeproj \
      -scheme CachedImageDemo \
      CODE_SIGNING_REQUIRED=NO \
      CODE_SIGNING_ALLOWED=NO
